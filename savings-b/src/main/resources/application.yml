server:
  port: 8082
spring:
#  config:
#    import: "optional:consul:"
  jpa:
#    show-sql: true
    generate-ddl: false
    open-in-view: false

    hibernate:
      ddl-auto: none

    properties:
      hibernate:
        format_sql: true

  datasource:
    url: jdbc:postgresql://localhost:5433/
    username: postgres
    password: a

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql: TRACE
      springframework:
        transaction: TRACE
        web:
          filter:
            CommonsRequestLoggingFilter: DEBUG
